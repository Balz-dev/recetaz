/**
 * @fileoverview Hook para manejar la lógica de la PWA.
 * 
 * Re-exporta la funcionalidad desde PWAProvider para mantener la organización.
 */

export { usePWA } from '@/shared/providers/PWAProvider';
export type { InstallGateState } from '@/shared/providers/PWAProvider';
